\* TEETH cfg: deliberately-WRONG 2-server-at-once change (MaxChangeDelta = 2).
\* A 2-server jump can move between configs whose majorities are DISJOINT -> QuorumOverlap (and
\* hence ElectionSafety / ConfigChangeSafety) MUST be VIOLATED here. This run is EXPECTED to
\* fail; it proves the checker has teeth — that the single-server rule (Membership.cfg) is not
\* vacuously satisfied. To reproduce: scripts/tlc.sh -config specs/Membership2.cfg specs/Membership.tla

SPECIFICATION Spec

CONSTANTS
    Server        = {s1, s2, s3, s4}
    Nil           = Nil
    Follower      = Follower
    Candidate     = Candidate
    Leader        = Leader
    MaxTerm       = 3
    MaxConfigs    = 3
    MaxChangeDelta = 2
    InitConfig    = {s1, s2, s3}

CONSTRAINT StateConstraint

\* The C4.2 deliverable invariants. QuorumOverlap (and ConfigChangeSafety) are expected to FAIL.
INVARIANT TypeOK
INVARIANT ElectionSafety
INVARIANT QuorumOverlap
INVARIANT ConfigChangeSafety
