# query — query / protocol layer (transaction-function model, wire protocol).
# Placeholder: bodies arrive Phase 6. INTERFACE lib so the tree configures.
add_library(lockstep_query INTERFACE)
# // bodies arrive Phase N
