# bench — benchmark harness (throughput, write/space amplification).
# Placeholder: bodies arrive Phase 3+. INTERFACE lib so the tree configures.
add_library(lockstep_bench INTERFACE)
# // bodies arrive Phase N
